# OpenCode AI 行为规范与约束

1. **执行边界**：
   
   - 任务完成后立即结束并向用户汇报，严禁自主扩展未要求的额外任务。
   - 严禁自行编写测试桩、冒烟测试脚本（如 cdp-smoke.mjs）或反复跑循环测试。

2. **Shell 与命令使用规范**：
   
   - 除非用户明确要求“请在终端运行某命令”，否则严禁主动调用 Shell / Bash 执行任何自动化探索或诊断。
   - 验证和测试工作默认交由人类用户手动在本地执行。

3. **代码修改规范**：
   
   - 优先通过文件修改工具精准应用 Diff，修改完毕后立即停止等待用户指令。
